# 已完成功能清单

## 用户微服务（Python gRPC）

| 功能             | 接口            | 说明                                     |
| ---------------- | --------------- | ---------------------------------------- |
| 获取用户列表     | GetUserList     | 支持分页查询                             |
| 通过 id 查询用户 | GetUserById     | 验证用户是否存在                         |
| 通过 email 查询  | GetUserByEmail  | 验证用户是否存在                         |
| 根据 mobile 查询 | GetUserByMobile | 登录/注册时验证用户是否存在              |
| 创建用户         | CreateUser      | 注册新用户，密码使用 PBKDF2 加密         |
| 更新用户信息     | UpdateUser      | 检查用户是否存在后更新                   |
| 密码校验         | CheckPassWord   | 使用 passlib 验证 PBKDF2 哈希            |
| Consul 服务注册  | -               | 启动时自动注册到 Consul                  |
| gRPC 健康检查    | -               | 注册健康检查服务，Consul 定期探测        |
| 优雅退出         | -               | 监听 SIGINT/SIGTERM 信号，退出时注销服务 |
| Nacos 配置管理   | -               | 从 Nacos 拉取配置，支持配置变更监听      |
| 动态端口分配     | -               | 默认端口为 0 时自动获取可用端口          |

## 商品微服务（Python gRPC）

### 商品服务

| 功能         | 接口              | 说明                                                          |
| ------------ | ----------------- | ------------------------------------------------------------- |
| 商品列表     | GoodsList         | 支持关键词搜索、价格/品牌/分类过滤、分页查询                  |
| 批量获取商品 | BatchGetGoods     | 根据商品 ID 列表批量查询（订单场景）                          |
| 创建商品     | CreateGoods       | 创建商品，关联分类和品牌                                      |
| 删除商品     | DeleteGoods       | 逻辑删除商品                                                  |
| 更新商品     | UpdateGoods       | 更新商品信息，关联分类和品牌                                  |
| 更新商品状态 | UpdateGoodsStatus | 仅更新 is_new/is_hot/on_sale，不触发分类/品牌校验和点击量增加 |
| 商品详情     | GetGoodsDetail    | 获取商品详情，点击量 +1                                       |

### 分类服务

| 功能         | 接口                | 说明                                    |
| ------------ | ------------------- | --------------------------------------- |
| 获取所有分类 | GetAllCategorysList | 返回全部分类列表 + 三级分类 JSON 树结构 |
| 获取子分类   | GetSubCategory      | 根据分类 ID 获取子分类列表              |
| 创建分类     | CreateCategory      | 创建分类，支持多级分类                  |
| 删除分类     | DeleteCategory      | 逻辑删除分类                            |
| 更新分类     | UpdateCategory      | 更新分类信息                            |

### 品牌服务

| 功能     | 接口        | 说明                     |
| -------- | ----------- | ------------------------ |
| 品牌列表 | BrandList   | 获取全部品牌列表         |
| 创建品牌 | CreateBrand | 创建品牌（名称唯一校验） |
| 删除品牌 | DeleteBrand | 逻辑删除品牌             |
| 更新品牌 | UpdateBrand | 更新品牌信息             |

### 轮播图服务

| 功能       | 接口         | 说明               |
| ---------- | ------------ | ------------------ |
| 轮播图列表 | BannerList   | 获取全部轮播图列表 |
| 创建轮播图 | CreateBanner | 创建轮播图         |
| 删除轮播图 | DeleteBanner | 逻辑删除轮播图     |
| 更新轮播图 | UpdateBanner | 更新轮播图信息     |

### 品牌分类服务

| 功能         | 接口                 | 说明                               |
| ------------ | -------------------- | ---------------------------------- |
| 品牌分类列表 | CategoryBrandList    | 分页获取品牌分类关联列表           |
| 按分类查品牌 | GetCategoryBrandList | 根据分类 ID 获取该分类下的所有品牌 |
| 创建品牌分类 | CreateCategoryBrand  | 创建品牌与分类的关联关系           |
| 删除品牌分类 | DeleteCategoryBrand  | 删除品牌分类关联                   |
| 更新品牌分类 | UpdateCategoryBrand  | 更新品牌分类关联                   |

### 商品服务通用能力

| 功能            | 说明                                               |
| --------------- | -------------------------------------------------- |
| Consul 服务注册 | 启动时自动注册到 Consul                            |
| gRPC 健康检查   | 注册健康检查服务，Consul 定期探测                  |
| 优雅退出        | 监听 SIGINT/SIGTERM 信号，退出时注销服务           |
| Nacos 配置管理  | 从 Nacos 拉取配置，支持配置变更监听                |
| 动态端口分配    | 默认端口为 0 时自动获取可用端口                    |
| 逻辑删除        | BaseModel 拦截删除操作，使用 is_deleted 字段软删除 |
| JSON 字段支持   | 商品图片、详情图使用 MySQL JSONField 存储          |

## 订单微服务（Python gRPC）

### 购物车服务

| 功能           | 接口            | 说明                               |
| -------------- | --------------- | ---------------------------------- |
| 获取购物车列表 | CartItemList    | 根据用户 ID 获取所有购物车条目     |
| 添加商品到购物车 | CreateCartItem  | 支持商品合并（已存在则累加数量）   |
| 更新购物车条目 | UpdateCartItem  | 修改商品数量和选中状态             |
| 删除购物车条目 | DeleteCartItem  | 逻辑删除购物车条目                 |

### 订单服务

| 功能           | 接口              | 说明                                                                 |
| -------------- | ----------------- | -------------------------------------------------------------------- |
| 创建订单       | CreateOrder       | 跨服务调用商品服务获取价格，调用库存服务扣减库存，事务保证数据一致性 |
| 获取订单列表   | OrderList         | 支持按用户 ID 过滤，分页查询                                         |
| 获取订单详情   | OrderDetail       | 返回订单基本信息 + 订单商品列表                                      |
| 更新订单状态   | UpdateOrderStatus | 根据订单号更新支付状态                                               |

### 订单服务核心能力

| 功能               | 说明                                                                 |
| ------------------ | -------------------------------------------------------------------- |
| 跨服务调用         | 通过 Consul 发现商品服务和库存服务，gRPC 调用获取商品信息和扣减库存 |
| 事务管理           | 使用 Peewee 的 `atomic()` 确保订单创建、库存扣减、购物车清理的原子性 |
| 订单号生成         | 基于时间戳 + 用户 ID + 随机数生成唯一订单号                          |
| 分布式锁           | 库存扣减使用 Redis 分布式锁防止超卖                                  |
| 乐观锁             | Inventory 模型包含 version 字段，支持并发控制                        |
| Consul 服务注册    | 启动时自动注册到 Consul                                              |
| gRPC 健康检查      | 注册健康检查服务，Consul 定期探测                                    |
| 优雅退出           | 监听 SIGINT/SIGTERM 信号，退出时注销服务                             |
| Nacos 配置管理     | 从 Nacos 拉取配置，支持配置变更监听                                  |
| 动态端口分配       | 默认端口为 0 时自动获取可用端口                                      |
| 逻辑删除           | BaseModel 拦截删除操作，使用 is_deleted 字段软删除                   |

## 库存微服务（Python gRPC）

### 库存服务

| 功能         | 接口        | 说明                                                                 |
| ------------ | ----------- | -------------------------------------------------------------------- |
| 设置库存     | SetInv      | 新增或更新商品库存记录                                               |
| 获取库存详情 | InvDetail   | 根据商品 ID 查询库存数量                                             |
| 扣减库存     | SellInv     | 使用 Redis 分布式锁防止超卖，事务保证库存扣减的原子性                |
| 归还库存     | RebackInv   | 订单超时或取消时归还库存，同样使用分布式锁                           |

### 库存服务核心能力

| 功能               | 说明                                                                 |
| ------------------ | -------------------------------------------------------------------- |
| Redis 分布式锁     | 使用 `python-redis-lock` 实现，防止并发扣减导致的超卖问题            |
| 锁自动续期         | `auto_renewal=True` 防止长事务执行期间锁过期                         |
| 事务管理           | 使用 Peewee 的 `atomic()` 确保库存操作的原子性                       |
| 乐观锁支持         | Inventory 模型包含 version 字段，可用于并发控制                      |
| 锁释放保证         | 使用 `try/finally` 确保锁在任何情况下都能正确释放                    |
| Consul 服务注册    | 启动时自动注册到 Consul                                              |
| gRPC 健康检查      | 注册健康检查服务，Consul 定期探测                                    |
| 优雅退出           | 监听 SIGINT/SIGTERM 信号，退出时注销服务                             |
| Nacos 配置管理     | 从 Nacos 拉取配置，支持配置变更监听                                  |
| 动态端口分配       | 默认端口为 0 时自动获取可用端口                                      |
| 逻辑删除           | BaseModel 拦截删除操作，使用 is_deleted 字段软删除                   |

## 用户 Web 服务（Go Gin）

### API 接口

| 功能       | 接口                 | 方法 | 说明                                           |
| ---------- | -------------------- | ---- | ---------------------------------------------- |
| 图片验证码 | /u/v1/base/captcha   | GET  | 生成 base64 格式验证码图片                     |
| 邮箱验证码 | /u/v1/base/send-code | POST | 发送注册验证码到邮箱，Redis 存储 5 分钟        |
| 密码登录   | /u/v1/user/pwd_login | POST | 手机号+密码+图片验证码登录，返回 JWT Token     |
| 用户注册   | /u/v1/user/register  | POST | 邮箱验证码+手机号+密码注册，注册成功返回 Token |
| 用户列表   | /u/v1/user/list      | GET  | 获取用户列表（需 JWT 认证 + 管理员权限）       |

### 中间件

| 中间件           | 说明                            |
| ---------------- | ------------------------------- |
| JWT 认证中间件   | 解析 x-token 头部，验证登录状态 |
| 管理员权限中间件 | 验证用户角色是否为管理员        |
| CORS 中间件      | 跨域请求支持                    |

### 工具模块

| 模块               | 说明                                     |
| ------------------ | ---------------------------------------- |
| 动态端口获取       | `utils/addr_port.go` 获取系统可用端口    |
| Redis 测试工具     | `api/redis_test/` 独立测试 Redis 连接    |
| 响应结构体         | `global/responselist/` 统一 API 响应格式 |
| 负载均衡 gRPC 连接 | `initialize/src_conn.go` 带负载均衡策略  |

## 商品 Web 服务（Go Gin）

### 商品接口

| 功能         | 接口                   | 方法   | 说明                                                           |
| ------------ | ---------------------- | ------ | -------------------------------------------------------------- |
| 商品列表     | /g/v1/goods            | GET    | 商品列表查询，支持价格区间/品牌/分类/关键词/热销/新品/分页过滤 |
| 新建商品     | /g/v1/goods            | POST   | 创建商品，关联分类和品牌（需 JWT + 管理员权限）                |
| 商品详情     | /g/v1/goods/:id        | GET    | 获取商品详情，点击量 +1                                        |
| 删除商品     | /g/v1/goods/:id        | DELETE | 逻辑删除商品（需 JWT + 管理员权限）                            |
| 获取库存     | /g/v1/goods/:id/stocks | GET    | 获取商品库存                                                   |
| 更新商品状态 | /g/v1/goods/:id        | PATCH  | 仅更新 is_new/is_hot/on_sale（需 JWT + 管理员权限）            |
| 更新商品     | /g/v1/goods/:id        | PUT    | 更新商品信息，关联分类和品牌（需 JWT + 管理员权限）            |

### 分类接口

| 功能     | 接口                | 方法   | 说明                   |
| -------- | ------------------- | ------ | ---------------------- |
| 分类列表 | /g/v1/categorys     | GET    | 获取全部分类列表       |
| 分类详情 | /g/v1/categorys/:id | GET    | 获取分类详情           |
| 新建分类 | /g/v1/categorys     | POST   | 创建分类，支持多级分类 |
| 删除分类 | /g/v1/categorys/:id | DELETE | 逻辑删除分类           |
| 更新分类 | /g/v1/categorys/:id | PUT    | 更新分类信息           |

### 品牌接口

| 功能     | 接口             | 方法   | 说明                     |
| -------- | ---------------- | ------ | ------------------------ |
| 品牌列表 | /g/v1/brands     | GET    | 获取全部品牌列表         |
| 新建品牌 | /g/v1/brands     | POST   | 创建品牌（名称唯一校验） |
| 删除品牌 | /g/v1/brands/:id | DELETE | 逻辑删除品牌             |
| 更新品牌 | /g/v1/brands/:id | PUT    | 更新品牌信息             |

### 轮播图接口

| 功能       | 接口              | 方法   | 说明                                  |
| ---------- | ----------------- | ------ | ------------------------------------- |
| 轮播图列表 | /g/v1/banners     | GET    | 获取全部轮播图列表                    |
| 新建轮播图 | /g/v1/banners     | POST   | 创建轮播图（需 JWT + 管理员权限）     |
| 删除轮播图 | /g/v1/banners/:id | DELETE | 逻辑删除轮播图（需 JWT + 管理员权限） |
| 更新轮播图 | /g/v1/banners/:id | PUT    | 更新轮播图信息（需 JWT + 管理员权限） |

### 品牌分类接口

| 功能         | 接口                     | 方法   | 说明                               |
| ------------ | ------------------------ | ------ | ---------------------------------- |
| 品牌分类列表 | /g/v1/categorybrands     | GET    | 分页获取品牌分类关联列表           |
| 按分类查品牌 | /g/v1/categorybrands/:id | GET    | 根据分类 ID 获取该分类下的所有品牌 |
| 新建品牌分类 | /g/v1/categorybrands     | POST   | 创建品牌与分类的关联关系           |
| 删除品牌分类 | /g/v1/categorybrands/:id | DELETE | 删除品牌分类关联                   |
| 更新品牌分类 | /g/v1/categorybrands/:id | PUT    | 更新品牌分类关联                   |

### 健康检查

| 功能     | 接口    | 方法 | 说明                                            |
| -------- | ------- | ---- | ----------------------------------------------- |
| 健康检查 | /health | GET  | Consul HTTP 健康检查端点，返回 200 表示服务正常 |

### 中间件

| 中间件           | 说明                            |
| ---------------- | ------------------------------- |
| JWT 认证中间件   | 解析 x-token 头部，验证登录状态 |
| 管理员权限中间件 | 验证用户角色是否为管理员        |
| CORS 中间件      | 跨域请求支持                    |

### 工具模块

| 模块               | 说明                                                                                                              |
| ------------------ | ----------------------------------------------------------------------------------------------------------------- |
| 动态端口获取       | `utils/addr_port.go` 获取系统可用端口                                                                             |
| Consul 服务注册    | `utils/register/consul/register.go` 接口化注册实现                                                                |
| 负载均衡 gRPC 连接 | `initialize/src_conn.go` 带负载均衡策略，单连接共享 5 个子服务客户端（Goods/Brand/Category/Banner/CategoryBrand） |

## 文件存储服务（Go Gin + MinIO）

### API 接口

| 功能         | 接口                | 方法   | 说明                                                      |
| ------------ | ------------------- | ------ | --------------------------------------------------------- |
| 获取上传凭证 | /oss/v1/oss/token   | GET    | 生成 MinIO 预签名 PUT 直传 URL，返回上传地址和访问地址    |
| 清理孤儿文件 | /oss/v1/oss/cleanup | DELETE | 清理超过指定时间未被业务服务认领的文件，支持 `hours` 参数 |
| 健康检查     | /health             | GET    | Consul HTTP 健康检查端点                                  |

### 中间件

| 中间件      | 说明         |
| ----------- | ------------ |
| CORS 中间件 | 跨域请求支持 |

### 工具模块

| 模块               | 说明                                                |
| ------------------ | --------------------------------------------------- |
| 动态端口获取       | `utils/addr_port.go` 获取系统可用端口               |
| MinIO 客户端初始化 | `initialize/miniooss.go` 自动创建桶、存储全局客户端 |
| Consul 服务注册    | `utils/register/consul/register.go` 接口化注册实现  |

### 前端页面

| 页面         | 说明                                       |
| ------------ | ------------------------------------------ |
| `index.html` | MinIO 文件直传测试页面，支持拖拽和点击上传 |
| `upload.js`  | 原生 HTML5 + XMLHttpRequest PUT 直传实现   |
